namespace CraftingEngine.Entities
{
    public sealed class Player : Person
    {
        public Player(string name) : base(name) { }

        // Future player‑specific fields (level, XP, etc.) go here.
    }
}